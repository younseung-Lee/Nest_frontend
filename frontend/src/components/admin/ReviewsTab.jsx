import React from 'react';
import ReviewManagement from './ReviewManagement';

const ReviewsTab = ({ isDarkMode }) => {
  return <ReviewManagement isDarkMode={isDarkMode} />;
};

export default ReviewsTab;